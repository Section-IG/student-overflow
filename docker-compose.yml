version: '3.4'

services:
  api:
    image: ${DOCKER_REGISTRY-}studentoverflow.api
    build:
      context: .
      dockerfile: src/StudentOverflow.API/Dockerfile
  db:
    image: "mcr.microsoft.com/mssql/server:2019-latest"
